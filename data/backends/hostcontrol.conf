# -*- coding: utf-8 -*-

module = 'HostControl'
config = {
    "opsiclientdPort":    4441,
    "hostRpcTimeout":     15,
    "resolveHostAddress": False,
    "maxConnections":     50,
    "broadcastAddresses": {
        # the format used is: 'address' : [port1, port2, ...]
        "255.255.255.255": [12287, ],
    }
}
