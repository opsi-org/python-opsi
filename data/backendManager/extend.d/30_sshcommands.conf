# -*- coding: utf-8 -*-


def createCommand(self, name, menuText, commands, needSudo, priority,tooltip=u'', parentMenu=None ):
	import json

	# if parentMenu==None:
		# parentMenu=u'Null'
	data={u'name':name,
		u'menuText':menuText,
		u'tooltip':tooltip,
		u'commands':commands,
		u'needSudo':needSudo,
		u'priority':priority,
		u'parentMenu':parentMenu}

	with open(self._getSshCommandFile(), 'w') as f:
		json.dump(data, f)
		# return True
	# return False

def readCommands(self):
	import json
	with open(self._getSshCommandFile(), 'r') as f:
		data=json.load(f)
		print data
		return data

# def updateCommand(self, name, menuText, commands, needSudo, priority,tooltip=u'', parentMenu=None )

def _getSshCommandFile(self):
	return u'/home/sucher/tmp/json/com.json'