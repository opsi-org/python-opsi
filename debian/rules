#!/usr/bin/make -f

OPSI_VERSION := $(shell dpkg-parsechangelog | grep "^Version:" | cut -d" " -f 2)

%:
	dh $@

override_dh_auto_install:
	dh_auto_install
	echo $(OPSI_VERSION) > debian/python-opsi/etc/opsi/version
